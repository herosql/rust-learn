// use rust_learn::fib_for;
// fn main() {
//     // println!("HelloWorld")
//     fib_for(10);
// }
fn main() {
    // let mut arr = vec![1, 2, 3];

    // arr.push(4);

    // // cache the last item
    // let last = arr.last();
    // // consume previously stored last item
    // println!("last: {:?}", last);
}
